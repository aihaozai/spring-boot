<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.myproject.mapper.ActHiTaskProcessMapper">
  <resultMap id="BaseResultMap" type="com.example.myproject.entity.activiti.ActHiTaskProcess">
    <!--
      WARNING - @mbg.generated
      This element is automatically generated by MyBatis Generator, do not modify.
    -->
    <id column="id" jdbcType="VARCHAR" property="id" />
    <result column="process_id" jdbcType="VARCHAR" property="processId" />
    <result column="table_id" jdbcType="VARCHAR" property="tableId" />
    <result column="task_name" jdbcType="VARCHAR" property="taskName" />
    <result column="launch_id" jdbcType="VARCHAR" property="launchId" />
    <result column="assignee" jdbcType="VARCHAR" property="assignee" />
    <result column="status" jdbcType="VARCHAR" property="status" />
    <result column="start_time" jdbcType="DATE" property="startTime" />
    <result column="end_time" jdbcType="DATE" property="endTime" />
    <result column="url" jdbcType="VARCHAR" property="url" />
  </resultMap>
  <resultMap id="ResultMap" type="hashmap" />
  <sql id="Base_Column_List">
    a.id, a.process_id, a.table_id, a.task_name, a.launch_id, a.assignee, a.status, a.agree_status, a.opinion, DATE_FORMAT(a.start_time,'%Y-%m-%d %H:%i:%s') as start_time,
    DATE_FORMAT(a.end_time,'%Y-%m-%d %H:%i:%s') as end_time ,a.url
  </sql>
     <select id="findAll" resultMap="ResultMap"  parameterType="object">
        select
        <include refid="Base_Column_List" />,
        ( select username  from user where id = a.launch_id) as launch_name,
        ( select username  from user where id = a.assignee) as assignee_name
         from act_hi_task_process a where 1=1
         <if test="status!=null">
             and a.status=#{status}
         </if>
         <if test="userId!=null">
             and a.assignee=#{userId}
         </if>
         <if test="tableId!=null">
             and a.table_id=#{tableId}
         </if>
     </select>
    <select id="getTaskCount" resultType="java.lang.Integer">
        select count(0) from act_hi_task_process a where 1=1
        <if test="id!=null">
            and a.assignee=#{id}
        </if>
        <if test="status!=null">
            and a.status=#{status}
        </if>
    </select>

</mapper>